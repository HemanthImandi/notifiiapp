<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:clickable="true"
    android:id="@+id/parentLayout"
    android:orientation="vertical">

    <include layout="@layout/toolbar_main" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/camera_icon_height"
        >


        <EditText
            android:id="@+id/editText_update_user_name"
            style="@style/EditTextStyle"
            android:layout_width="match_parent"
            android:layout_height="@dimen/edittext_height"
            android:imeOptions="actionDone"
            android:layout_gravity="center"
            android:contentDescription="@string/text_new_username"
            android:hint="@string/text_new_username"
          />

        <Button
            android:id="@+id/button_update_username_submit"
            style="@style/ButtonStyle"
            android:layout_width="match_parent"
            android:layout_height="@dimen/button_height"
            android:layout_gravity="center"
            android:text="@string/action_submit"
            android:layout_marginTop="@dimen/medium_above_padding"
          />

    </LinearLayout>

    <Button
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:importantForAccessibility="no"
        android:background="@android:color/transparent" />

</LinearLayout>